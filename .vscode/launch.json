{
    "version": "0.2.0",
    "configurations": [
        {
            "name": ".NET Core Launch (console)",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build",
            "program": "${workspaceFolder}/Project/bin/Debug/net8.0/Project.dll", // <--- Uses the workspace folder basename dynamically
            "args": [],
            "cwd": "${workspaceFolder}/Project",
            "stopAtEntry": false,
            "console": "integratedTerminal"
        }
    ]
}